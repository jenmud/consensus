{{ define "body" }}
<div class="hero min-h-screen bg-base-200">
    <div class="hero-content flex-col lg:flex-row-reverse">
        <div class="text-center lg:text-left">
            <h1 class="text-5xl font-bold">Consensus</h1>
            <p class="py-6">Register a new user</p>
        </div>
        <div class="card shrink-0 w-full max-w-sm shadow-2xl bg-base-100">
            <form class="card-body" hx-post="/register" hx-target="#body" hx-on="htmx:responseError alert('whoops')">
                <div class="form-control">
                    <input name="first_name" type="input" placeholder="first name" class="input input-bordered" required />
                </div>
                <div class="form-control">
                    <input name="last_name" type="input" placeholder="last name" class="input input-bordered" required />
                </div>
                <div class="form-control">
                    <input name="email" type="email" placeholder="email" class="input input-bordered" required />
                </div>
                <div class="form-control">
                    <!-- <input name="password" type="password"
                        title="Must contain at least one number and one uppercase and lowercase letter, and at least 4 or more characters"
                        pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{4,}" placeholder="password"
                        class="input input-bordered" required /> -->
                    <input name="password" type="password"
                        title="Must contain at least one number and one uppercase and lowercase letter, and at least 4 or more characters"
                        class="input input-bordered" required />
                </div>
                <div class="form-control">
                    <input name="confirm_password" type="password" placeholder="confirm password" class="input input-bordered" required />
                </div>
                <div class="flex gap-2 self-end mt-4">
                    <div class="form-control">
                        <button class="btn btn-primary btn-sm">Create</button>
                    </div>
                    <button class="btn btn-error btn-sm" hx-get="/login" hx-target="#body"
                        hx-swap="outerHTML">Cancel</button>
                </div>
            </form>
        </div>
    </div>
</div>
{{ end }}